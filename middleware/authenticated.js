export default function ({ store, redirect }) {
	//If the user is not authenticated
	// console.log(store.state.options.authenticated);
	// if (store.state.options.authenticated.join("") !== '061218') {
	// 	return redirect('/')
	// }
}